# 任务验收卡 2.2: 收件人管理功能

**任务编号**: 2.2
**所属 Sprint**: Sprint 2 - 写信面板开发
**验收日期**: 待定
**验收人**: AI 工程师(自动验收) + 用户(人工验收)

---

## 验收流程说明

本任务采用两阶段验收:

1. **阶段一: AI 自动验收** - AI 工程师执行代码检查、编译测试、静态分析
2. **阶段二: 人工验收** - 用户执行 UI 操作测试和功能验证

只有 AI 自动验收通过后,才能进入人工验收阶段。

---

## 📋 阶段一: AI 自动验收

**执行人**: AI 工程师
**执行时机**: 代码提交后立即执行

### 1.1 交付物检查

**检查项**:
- [ ] `src/compose/recipient_manager.ts` 文件已创建
- [ ] `extension/html/compose.html` 已更新,包含收件人管理 UI
- [ ] `src/compose.ts` 已集成收件人管理逻辑
- [ ] 所有文件路径正确,无拼写错误

**验收方法**:
```bash
# 检查文件是否存在
ls -la src/compose/recipient_manager.ts
ls -la extension/html/compose.html
ls -la src/compose.ts
```

---

### 1.2 TypeScript 编译检查

**检查项**:
- [ ] TypeScript 编译无错误
- [ ] 无类型定义缺失
- [ ] 接口定义完整

**验收方法**:
```bash
npm run build
# 或
tsc --noEmit
```

**期望结果**: 编译成功,无错误输出

---

### 1.3 代码质量检查

#### 1.3.1 代码结构检查

**检查项**:
- [ ] `RecipientManager` 类已定义
- [ ] 包含 `Recipient` 接口定义
- [ ] 实现了所有必需方法:
  - `addRecipient(email: string): boolean`
  - `removeRecipient(email: string): void`
  - `getRecipients(): Recipient[]`
  - `getValidEmails(): string[]`
  - `validateEmail(email: string): boolean` (静态方法)
  - `clear(): void`
  - `onChange(callback): void`
  - `render(container: HTMLElement): void`

**验收方法**:
```bash
# 检查类和方法定义
grep -n "export class RecipientManager" src/compose/recipient_manager.ts
grep -n "addRecipient" src/compose/recipient_manager.ts
grep -n "removeRecipient" src/compose/recipient_manager.ts
grep -n "getValidEmails" src/compose/recipient_manager.ts
```

---

#### 1.3.2 邮箱验证逻辑检查

**检查项**:
- [ ] 邮箱验证正则表达式存在
- [ ] 验证逻辑合理(包含 @ 和域名)
- [ ] 邮箱地址会转小写
- [ ] 邮箱地址会去除首尾空格

**验收方法**:
```typescript
// 在 src/compose/recipient_manager.ts 中检查
// 应该包含类似这样的正则表达式
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
```

---

#### 1.3.3 错误处理检查

**检查项**:
- [ ] 空邮箱地址被拒绝
- [ ] 无效邮箱格式被拒绝
- [ ] 重复邮箱被拒绝
- [ ] 所有错误都有日志记录

**验收方法**:
```bash
# 检查错误处理代码
grep -n "console.error\|console.warn" src/compose/recipient_manager.ts
```

---

### 1.4 UI 代码检查

**检查项**:
- [ ] compose.html 包含收件人输入框
- [ ] compose.html 包含添加按钮
- [ ] compose.html 包含收件人列表容器
- [ ] compose.html 包含错误提示区域
- [ ] CSS 样式已定义

**验收方法**:
```bash
# 检查 HTML 元素
grep -n "recipient-input" extension/html/compose.html
grep -n "add-recipient-btn" extension/html/compose.html
grep -n "recipient-list" extension/html/compose.html
grep -n "recipient-error" extension/html/compose.html
```

---

### 1.5 集成检查

**检查项**:
- [ ] compose.ts 导入了 RecipientManager
- [ ] compose.ts 初始化了 recipientManager 实例
- [ ] compose.ts 调用了 render() 方法
- [ ] compose.ts 设置了 onChange 回调
- [ ] 导出了 getRecipients() 函数供外部使用

**验收方法**:
```bash
# 检查集成代码
grep -n "import.*RecipientManager" src/compose.ts
grep -n "recipientManager = new RecipientManager" src/compose.ts
grep -n "recipientManager.render" src/compose.ts
grep -n "getRecipients" src/compose.ts
```

---

### 1.6 代码规范检查

**检查项**:
- [ ] 代码缩进一致
- [ ] 变量命名符合规范(驼峰命名)
- [ ] 关键方法有注释
- [ ] 无明显的代码重复

**验收方法**:
```bash
# 运行 ESLint (如果配置了)
npm run lint
```

---

### AI 自动验收结论

**验收结果**: [ ] 通过  [ ] 不通过

**通过标准**: 上述所有检查项全部通过

**如果不通过**:
- 列出未通过的检查项
- 说明需要修复的问题
- 不进入人工验收阶段

**如果通过**:
- 记录验收时间
- 通知用户可以开始人工验收
- 提供人工验收指引

---

## 👤 阶段二: 人工验收

**执行人**: 用户
**前置条件**: AI 自动验收已通过

### 2.1 环境准备

**操作步骤**:
1. 确保扩展已重新加载
2. 打开 Chrome 浏览器
3. 访问 Gmail (https://mail.google.com)
4. 打开 DevTools (F12)

---

### 2.2 基础功能测试

#### TC-01: 收件人输入框显示

**测试目标**: 验证收件人输入框正常显示

**测试步骤**:
1. 点击 FAB 按钮打开写信面板
2. 观察收件人输入区域

**期望结果**:
- [ ] 收件人输入框正常显示
- [ ] 有"收件人:"标签
- [ ] 有输入框和"添加"按钮
- [ ] 有"已添加收件人"区域
- [ ] 显示收件人计数(初始为 0)

**截图**: [粘贴截图]

---

#### TC-02: 添加有效收件人

**测试目标**: 验证能成功添加有效邮箱地址

**测试步骤**:
1. 在收件人输入框中输入 `test@example.com`
2. 点击"添加"按钮

**期望结果**:
- [ ] 收件人成功添加到列表
- [ ] 输入框自动清空
- [ ] 收件人列表显示该邮箱
- [ ] 收件人计数更新为 1
- [ ] 无错误提示

**截图**: [粘贴截图]

---

#### TC-03: 添加多个收件人

**测试目标**: 验证能添加多个收件人

**测试步骤**:
1. 依次添加以下邮箱:
   - `user1@example.com`
   - `user2@example.com`
   - `user3@example.com`

**期望结果**:
- [ ] 所有收件人都成功添加
- [ ] 收件人列表显示 3 个邮箱
- [ ] 收件人计数显示 3
- [ ] 每个收件人都有删除按钮

**截图**: [粘贴截图]

---

#### TC-04: 回车键添加收件人

**测试目标**: 验证按回车键能添加收件人

**测试步骤**:
1. 在输入框中输入 `enter@example.com`
2. 按回车键(Enter)

**期望结果**:
- [ ] 收件人成功添加
- [ ] 输入框自动清空
- [ ] 效果与点击"添加"按钮相同

**截图**: [粘贴截图]

---

### 2.3 验证功能测试

#### TC-05: 拒绝空邮箱地址

**测试目标**: 验证空邮箱地址被拒绝

**测试步骤**:
1. 不输入任何内容
2. 点击"添加"按钮

**期望结果**:
- [ ] 显示错误提示:"请输入邮箱地址"
- [ ] 收件人未被添加
- [ ] 错误提示为红色

**截图**: [粘贴截图]

---

#### TC-06: 拒绝无效邮箱格式

**测试目标**: 验证无效邮箱格式被拒绝

**测试步骤**:
1. 依次尝试输入以下无效邮箱:
   - `invalid-email` (无 @)
   - `test@` (无域名)
   - `@example.com` (无用户名)
   - `test @example.com` (包含空格)
2. 每次都点击"添加"按钮

**期望结果**:
- [ ] 所有无效邮箱都被拒绝
- [ ] 显示错误提示:"邮箱格式不正确"
- [ ] 收件人未被添加

**截图**: [粘贴每种情况的截图]

---

#### TC-07: 拒绝重复邮箱

**测试目标**: 验证重复邮箱被拒绝

**测试步骤**:
1. 添加 `duplicate@example.com`
2. 再次尝试添加 `duplicate@example.com`

**期望结果**:
- [ ] 第一次添加成功
- [ ] 第二次添加被拒绝
- [ ] 显示错误提示:"该收件人已存在"
- [ ] 列表中只有一个该邮箱

**截图**: [粘贴截图]

---

#### TC-08: 邮箱地址规范化

**测试目标**: 验证邮箱地址自动转小写和去空格

**测试步骤**:
1. 输入 `  TEST@EXAMPLE.COM  ` (包含大写和首尾空格)
2. 点击"添加"按钮

**期望结果**:
- [ ] 收件人成功添加
- [ ] 列表中显示为 `test@example.com` (小写,无空格)

**截图**: [粘贴截图]

---

### 2.4 删除功能测试

#### TC-09: 删除收件人

**测试目标**: 验证能删除收件人

**测试步骤**:
1. 添加 3 个收件人
2. 点击第二个收件人的删除按钮(×)

**期望结果**:
- [ ] 该收件人从列表中移除
- [ ] 收件人计数更新为 2
- [ ] 其他收件人不受影响

**截图**: [粘贴删除前后的截图]

---

#### TC-10: 删除所有收件人

**测试目标**: 验证删除所有收件人后的状态

**测试步骤**:
1. 添加 2 个收件人
2. 依次删除所有收件人

**期望结果**:
- [ ] 所有收件人都被删除
- [ ] 收件人计数显示 0
- [ ] 列表显示"暂无收件人"

**截图**: [粘贴截图]

---

### 2.5 UI/UX 测试

#### TC-11: 错误提示自动消失

**测试目标**: 验证输入时错误提示自动消失

**测试步骤**:
1. 触发一个错误提示(如输入无效邮箱)
2. 在输入框中输入任意字符

**期望结果**:
- [ ] 错误提示立即消失
- [ ] 输入框恢复正常状态

**截图**: [粘贴截图]

---

#### TC-12: 删除按钮 Hover 效果

**测试目标**: 验证删除按钮的交互效果

**测试步骤**:
1. 添加一个收件人
2. 将鼠标悬停在删除按钮上

**期望结果**:
- [ ] 删除按钮背景色变化
- [ ] 删除按钮颜色变为红色
- [ ] 鼠标指针变为手型

**截图**: [粘贴 hover 状态截图]

---

#### TC-13: 收件人列表滚动

**测试目标**: 验证收件人列表滚动功能

**测试步骤**:
1. 添加 10 个收件人
2. 观察收件人列表

**期望结果**:
- [ ] 列表高度有限制(max-height: 150px)
- [ ] 出现滚动条
- [ ] 滚动流畅

**截图**: [粘贴截图]

---

### 2.6 集成测试

#### TC-14: 获取收件人列表

**测试目标**: 验证能通过 API 获取收件人列表

**测试步骤**:
1. 添加 3 个收件人
2. 在 DevTools Console 中执行:
   ```javascript
   window.getRecipients()
   ```

**期望结果**:
- [ ] 返回包含 3 个邮箱地址的数组
- [ ] 邮箱地址格式正确
- [ ] 顺序与添加顺序一致

**Console 输出**: [复制输出内容]

---

#### TC-15: onChange 回调触发

**测试目标**: 验证收件人变化时回调被触发

**测试步骤**:
1. 打开 DevTools Console
2. 添加一个收件人
3. 删除一个收件人
4. 观察 Console 日志

**期望结果**:
- [ ] 添加时有 "Recipients changed" 日志
- [ ] 删除时有 "Recipients changed" 日志
- [ ] 日志包含当前收件人列表

**Console 输出**: [复制输出内容]

---

### 2.7 兼容性测试

#### TC-16: 多邮箱站点兼容性

**测试目标**: 验证在不同邮箱站点的兼容性

**测试步骤**:
1. 依次在以下站点测试基本功能:
   - Gmail (https://mail.google.com)
   - QQ 邮箱 (https://mail.qq.com)
   - 163 邮箱 (https://mail.163.com)
   - Outlook (https://outlook.live.com)
2. 每个站点都测试添加和删除收件人

**期望结果**:
- [ ] Gmail: 功能正常
- [ ] QQ 邮箱: 功能正常
- [ ] 163 邮箱: 功能正常
- [ ] Outlook: 功能正常
- [ ] 样式在所有站点一致

**截图**: [粘贴每个站点的截图]

---

## 📊 验收结果汇总

### AI 自动验收结果

**验收日期**: ___________
**验收人**: AI 工程师
**验收结果**: [ ] 通过  [ ] 不通过

**通过的检查项**: _____ / _____

**未通过的检查项**:
1.
2.
3.

---

### 人工验收结果

**验收日期**: ___________
**验收人**: 用户
**验收结果**: [ ] 通过  [ ] 不通过

**通过的测试用例**: _____ / 16

**失败的测试用例**:
1.
2.
3.

---

## 🐛 发现的问题

### 问题列表

1. **问题描述**:
   - 严重程度: [ ] P0  [ ] P1  [ ] P2
   - 复现步骤:
   - 期望行为:
   - 实际行为:

2. **问题描述**:
   - 严重程度: [ ] P0  [ ] P1  [ ] P2
   - 复现步骤:
   - 期望行为:
   - 实际行为:

---

## ✅ 最终验收决策

### 验收通过条件

- AI 自动验收全部通过
- 人工验收测试用例通过率 ≥ 90%
- 无 P0 级别的 Bug
- P1 级别的 Bug ≤ 2 个

### 验收决策

**最终结论**: [ ] 通过  [ ] 不通过  [ ] 有条件通过

**决策说明**:

**后续行动**:
- [ ] 修复发现的 Bug
- [ ] 更新开发进度管理文档
- [ ] 开始任务 2.3 开发

---

**验收人签字**:
- AI 工程师: ___________
- 用户: ___________

**项目负责人签字**: ___________

---

## 📚 相关文档

- [2.2_任务执行卡_收件人管理功能.md](./2.2_任务执行卡_收件人管理功能.md) - 任务执行卡
- [2.Sprint2_写信面板开发.md](./2.Sprint2_写信面板开发.md) - Sprint 2 规划
- [开发进度管理.md](../开发进度管理.md) - 整体进度跟踪

---

**备注**: 本验收卡采用两阶段验收流程,AI 自动验收通过后才能进入人工验收。
