# 任务验收卡 2.4: 发送流程集成

**任务编号**: 2.4
**所属 Sprint**: Sprint 2 - 写信面板开发
**验收日期**: 待定
**验收人**: AI 工程师(自动验收) + 用户(人工验收)

---

## 验收流程说明

本任务采用两阶段验收:

1. **阶段一: AI 自动验收** - AI 工程师执行代码检查、编译测试、静态分析
2. **阶段二: 人工验收** - 用户执行完整流程测试和集成验证

只有 AI 自动验收通过后,才能进入人工验收阶段。

---

## 📋 阶段一: AI 自动验收

**执行人**: AI 工程师
**执行时机**: 代码提交后立即执行

### 1.1 交付物检查

**检查项**:
- [ ] `src/compose/sender.ts` 文件已创建
- [ ] `extension/html/compose.html` 已更新,包含主题输入框和发送按钮
- [ ] `src/compose.ts` 已集成发送流程
- [ ] `src/fab_injector/message_bus.ts` 已更新,处理 SEND_EMAIL 消息
- [ ] 所有文件路径正确,无拼写错误

**验收方法**:
```bash
# 检查文件是否存在
ls -la src/compose/sender.ts
ls -la extension/html/compose.html
ls -la src/compose.ts
ls -la src/fab_injector/message_bus.ts
```

---

### 1.2 TypeScript 编译检查

**检查项**:
- [ ] TypeScript 编译无错误
- [ ] 无类型定义缺失
- [ ] 接口定义完整

**验收方法**:
```bash
npm run build
# 或
tsc --noEmit
```

**期望结果**: 编译成功,无错误输出

---

### 1.3 代码结构检查

#### 1.3.1 Sender 模块检查

**检查项**:
- [ ] `Sender` 类已定义
- [ ] `SendOptions` 接口已定义
- [ ] `SendResult` 接口已定义
- [ ] `send()` 静态方法已实现
- [ ] `validateInput()` 私有方法已实现
- [ ] `buildSendMessage()` 私有方法已实现
- [ ] `sendToHost()` 私有方法已实现

**验收方法**:
```bash
# 检查类和方法定义
grep -n "export class Sender" src/compose/sender.ts
grep -n "interface SendOptions" src/compose/sender.ts
grep -n "interface SendResult" src/compose/sender.ts
grep -n "static async send" src/compose/sender.ts
grep -n "validateInput" src/compose/sender.ts
grep -n "buildSendMessage" src/compose/sender.ts
grep -n "sendToHost" src/compose/sender.ts
```

---

#### 1.3.2 消息协议检查

**检查项**:
- [ ] SEND_EMAIL 消息格式正确
- [ ] 包含 recipients 字段
- [ ] 包含 subject 字段
- [ ] 包含 encryptedContent 字段
- [ ] 包含 metadata 对象

**验收方法**:
```bash
# 检查消息构建逻辑
grep -A 10 "buildSendMessage" src/compose/sender.ts
```

---

### 1.4 输入验证检查

**检查项**:
- [ ] 验证收件人不为空
- [ ] 验证主题不为空
- [ ] 验证内容不为空
- [ ] 所有验证都有错误提示

**验收方法**:
```bash
# 检查验证逻辑
grep -n "validateInput" src/compose/sender.ts
grep -A 20 "validateInput" src/compose/sender.ts
```

---

### 1.5 UI 代码检查

**检查项**:
- [ ] compose.html 包含主题输入框
- [ ] compose.html 包含发送按钮
- [ ] compose.html 包含取消按钮
- [ ] compose.html 包含发送状态提示
- [ ] CSS 样式已定义

**验收方法**:
```bash
# 检查 HTML 元素
grep -n "subject-input" extension/html/compose.html
grep -n "send-btn" extension/html/compose.html
grep -n "cancel-btn" extension/html/compose.html
grep -n "send-status" extension/html/compose.html
```

---

### 1.6 集成检查

**检查项**:
- [ ] compose.ts 导入了 Sender
- [ ] compose.ts 实现了 handleSend 函数
- [ ] compose.ts 实现了 handleCancel 函数
- [ ] compose.ts 实现了 clearForm 函数
- [ ] compose.ts 绑定了按钮事件
- [ ] message_bus.ts 处理 SEND_EMAIL 消息

**验收方法**:
```bash
# 检查集成代码
grep -n "import.*Sender" src/compose.ts
grep -n "async function handleSend" src/compose.ts
grep -n "function handleCancel" src/compose.ts
grep -n "function clearForm" src/compose.ts
grep -n "initButtons" src/compose.ts
grep -n "case 'SEND_EMAIL'" src/fab_injector/message_bus.ts
```

---

### 1.7 错误处理检查

**检查项**:
- [ ] 发送失败有错误返回
- [ ] 超时有处理机制
- [ ] 所有异常都被捕获
- [ ] 错误信息记录到日志
- [ ] 发送按钮状态管理正确

**验收方法**:
```bash
# 检查错误处理代码
grep -n "try {" src/compose/sender.ts
grep -n "catch" src/compose/sender.ts
grep -n "console.error" src/compose/sender.ts
grep -n "SEND_TIMEOUT" src/compose/sender.ts
```

---

### 1.8 postMessage 通信检查

**检查项**:
- [ ] 使用 window.parent.postMessage 发送消息
- [ ] 监听 message 事件接收响应
- [ ] 验证响应的 origin
- [ ] 有超时机制
- [ ] 清理事件监听器

**验收方法**:
```bash
# 检查 postMessage 代码
grep -n "window.parent.postMessage" src/compose/sender.ts
grep -n "window.addEventListener('message'" src/compose/sender.ts
grep -n "event.origin" src/compose/sender.ts
grep -n "setTimeout" src/compose/sender.ts
grep -n "removeEventListener" src/compose/sender.ts
```

---

### 1.9 代码规范检查

**检查项**:
- [ ] 代码缩进一致
- [ ] 变量命名符合规范
- [ ] 关键方法有注释
- [ ] 无明显的代码重复
- [ ] 异步函数正确使用 async/await

**验收方法**:
```bash
# 运行 ESLint (如果配置了)
npm run lint
```

---

### AI 自动验收结论

**验收结果**: [ ] 通过  [ ] 不通过

**通过标准**: 上述所有检查项全部通过

**如果不通过**:
- 列出未通过的检查项
- 说明需要修复的问题
- 不进入人工验收阶段

**如果通过**:
- 记录验收时间
- 通知用户可以开始人工验收
- 提供人工验收指引

---

## 👤 阶段二: 人工验收

**执行人**: 用户
**前置条件**: AI 自动验收已通过

### 2.1 环境准备

**操作步骤**:
1. 确保扩展已重新加载
2. 打开 Chrome 浏览器
3. 访问 Gmail (https://mail.google.com)
4. 打开 DevTools (F12)

---

### 2.2 UI 显示测试

#### TC-01: 主题输入框显示

**测试目标**: 验证主题输入框正常显示

**测试步骤**:
1. 点击 FAB 按钮打开写信面板
2. 观察主题输入区域

**期望结果**:
- [ ] 主题输入框正常显示
- [ ] 有"主题:"标签
- [ ] 输入框有占位符文本
- [ ] 输入框样式美观

**截图**: [粘贴截图]

---

#### TC-02: 发送和取消按钮显示

**测试目标**: 验证操作按钮正常显示

**测试步骤**:
1. 观察写信面板底部的按钮区域

**期望结果**:
- [ ] 发送按钮正常显示
- [ ] 取消按钮正常显示
- [ ] 按钮样式美观
- [ ] 按钮布局合理

**截图**: [粘贴截图]

---

### 2.3 基础功能测试

#### TC-03: 完整发送流程

**测试目标**: 验证完整的发送流程

**测试步骤**:
1. 添加收件人: `test@example.com`
2. 输入主题: `Test Email`
3. 输入邮件内容: `This is a test email.`
4. 点击"发送"按钮
5. 观察发送状态提示
6. 查看 DevTools Console 日志

**期望结果**:
- [ ] 显示"正在发送邮件..."状态
- [ ] 显示"邮件发送成功!"提示
- [ ] 状态提示自动消失(3秒后)
- [ ] Console 中有完整的发送日志
- [ ] Console 中有 SEND_EMAIL 消息日志
- [ ] 表单自动清空

**截图**: [粘贴发送过程截图]

**Console 输出**: [复制输出内容]

---

#### TC-04: 取消功能

**测试目标**: 验证取消按钮能关闭面板

**测试步骤**:
1. 打开写信面板
2. 输入一些内容
3. 点击"取消"按钮

**期望结果**:
- [ ] 面板关闭
- [ ] 面板以侧滑动画关闭
- [ ] Console 中有 CLOSE_PANEL 消息日志

**截图**: [粘贴截图]

---

### 2.4 验证功能测试

#### TC-05: 拒绝空主题

**测试目标**: 验证空主题被拒绝

**测试步骤**:
1. 添加收件人
2. 输入邮件内容
3. 不输入主题
4. 点击"发送"按钮

**期望结果**:
- [ ] 显示错误提示:"请输入邮件主题"
- [ ] 发送失败
- [ ] 表单不清空

**截图**: [粘贴截图]

---

#### TC-06: 拒绝空内容

**测试目标**: 验证空内容被拒绝

**测试步骤**:
1. 添加收件人
2. 输入主题
3. 不输入邮件内容
4. 点击"发送"按钮

**期望结果**:
- [ ] 显示错误提示:"邮件内容不能为空"
- [ ] 发送失败
- [ ] 表单不清空

**截图**: [粘贴截图]

---

#### TC-07: 拒绝无收件人

**测试目标**: 验证无收件人被拒绝

**测试步骤**:
1. 不添加收件人
2. 输入主题和内容
3. 点击"发送"按钮

**期望结果**:
- [ ] 显示错误提示:"请至少添加一个收件人"
- [ ] 发送失败
- [ ] 表单不清空

**截图**: [粘贴截图]

---

### 2.5 postMessage 通信测试

#### TC-08: 发送消息到宿主

**测试目标**: 验证消息正确发送到宿主页面

**测试步骤**:
1. 完成一次完整发送
2. 在 DevTools Console (top context) 中查看日志
3. 确认收到 SEND_EMAIL 消息

**期望结果**:
- [ ] 宿主页面收到 SEND_EMAIL 消息
- [ ] 消息包含 recipients 字段
- [ ] 消息包含 subject 字段
- [ ] 消息包含 encryptedContent 字段
- [ ] 消息包含 metadata 对象
- [ ] Console 有 "Received SEND_EMAIL message" 日志

**Console 输出**: [复制宿主页面的日志]

---

#### TC-09: 接收宿主响应

**测试目标**: 验证能接收宿主的响应

**测试步骤**:
1. 完成一次完整发送
2. 观察发送状态变化
3. 查看 Console 日志

**期望结果**:
- [ ] Iframe 收到 SEND_EMAIL_RESPONSE 消息
- [ ] 响应包含 success 字段
- [ ] 响应包含 messageId 字段
- [ ] Console 有 "Sent SEND_EMAIL_RESPONSE" 日志

**Console 输出**: [复制输出内容]

---

#### TC-10: 发送超时处理

**测试目标**: 验证发送超时的处理

**测试步骤**:
1. 在 message_bus.ts 中临时注释掉响应代码
2. 尝试发送邮件
3. 等待 30 秒
4. 观察超时提示

**期望结果**:
- [ ] 30 秒后显示超时错误
- [ ] 错误提示:"发送超时,请重试"
- [ ] 发送按钮恢复可用状态

**截图**: [粘贴截图]

**注意**: 测试完成后恢复 message_bus.ts 的代码

---

### 2.6 集成测试

#### TC-11: 与编辑器集成

**测试目标**: 验证与编辑器的集成

**测试步骤**:
1. 在编辑器中输入带格式的内容
2. 完成发送
3. 查看发送的消息内容

**期望结果**:
- [ ] 能获取编辑器的 HTML 内容
- [ ] 格式被保留
- [ ] 内容正确加密
- [ ] 发送成功

**Console 输出**: [复制消息内容]

---

#### TC-12: 与收件人管理集成

**测试目标**: 验证与收件人管理的集成

**测试步骤**:
1. 添加多个收件人
2. 完成发送
3. 查看发送的消息中的收件人列表

**期望结果**:
- [ ] 能获取所有收件人
- [ ] 收件人列表正确
- [ ] 收件人顺序保持一致

**Console 输出**: [复制收件人列表]

---

#### TC-13: 与加密模块集成

**测试目标**: 验证与加密模块的集成

**测试步骤**:
1. 完成一次发送
2. 查看发送的 encryptedContent 字段

**期望结果**:
- [ ] encryptedContent 是序列化的加密包
- [ ] 可以反序列化为加密包对象
- [ ] 加密包格式正确

**Console 输出**: [复制加密包内容]

---

### 2.7 UI/UX 测试

#### TC-14: 发送按钮状态管理

**测试目标**: 验证发送按钮的状态管理

**测试步骤**:
1. 点击发送按钮
2. 观察按钮状态变化

**期望结果**:
- [ ] 发送中按钮被禁用
- [ ] 按钮文字或样式有变化
- [ ] 发送完成后按钮恢复
- [ ] 发送失败后按钮恢复

**截图**: [粘贴不同状态的截图]

---

#### TC-15: 发送状态动画

**测试目标**: 验证发送状态的动画效果

**测试步骤**:
1. 执行发送
2. 观察状态提示的显示和消失

**期望结果**:
- [ ] 状态提示平滑显示
- [ ] 图标和文字清晰
- [ ] 成功提示自动消失
- [ ] 动画流畅无卡顿

**截图**: [粘贴状态提示截图]

---

#### TC-16: 表单清空功能

**测试目标**: 验证发送成功后表单清空

**测试步骤**:
1. 填写完整表单
2. 发送成功
3. 观察表单状态

**期望结果**:
- [ ] 主题输入框清空
- [ ] 编辑器内容清空
- [ ] 收件人列表清空
- [ ] 所有字段恢复初始状态

**截图**: [粘贴清空后的截图]

---

### 2.8 错误处理测试

#### TC-17: 发送失败处理

**测试目标**: 验证发送失败的处理

**测试步骤**:
1. 在 message_bus.ts 中临时修改响应为失败
2. 尝试发送邮件
3. 观察错误提示

**期望结果**:
- [ ] 显示发送失败提示
- [ ] 错误信息清晰
- [ ] 表单不清空
- [ ] 可以重新发送

**截图**: [粘贴截图]

**注意**: 测试完成后恢复 message_bus.ts 的代码

---

#### TC-18: 网络异常处理

**测试目标**: 验证网络异常的处理

**测试步骤**:
1. 在发送过程中模拟网络中断
2. 观察错误提示

**期望结果**:
- [ ] 有友好的错误提示
- [ ] 不会崩溃
- [ ] 可以重试

**截图**: [粘贴截图]

---

### 2.9 性能测试

#### TC-19: 发送流程性能

**测试目标**: 验证发送流程的性能

**测试步骤**:
1. 在 Console 中测试发送性能:
   ```javascript
   const start = performance.now();
   // 点击发送按钮
   // 等待发送完成
   const end = performance.now();
   console.log('Send time:', (end - start).toFixed(2), 'ms');
   ```

**期望结果**:
- [ ] 发送流程总耗时 < 3s
- [ ] 无明显延迟
- [ ] UI 响应流畅

**Console 输出**: [复制时间]

---

### 2.10 兼容性测试

#### TC-20: 多邮箱站点兼容性

**测试目标**: 验证在不同邮箱站点的兼容性

**测试步骤**:
1. 依次在以下站点测试完整发送流程:
   - Gmail (https://mail.google.com)
   - QQ 邮箱 (https://mail.qq.com)
   - 163 邮箱 (https://mail.163.com)
   - Outlook (https://outlook.live.com)
2. 每个站点都完成一次发送

**期望结果**:
- [ ] Gmail: 发送功能正常
- [ ] QQ 邮箱: 发送功能正常
- [ ] 163 邮箱: 发送功能正常
- [ ] Outlook: 发送功能正常
- [ ] 所有站点 UI 一致
- [ ] 所有站点通信正常

**截图**: [粘贴每个站点的截图]

---

### 2.11 端到端测试

#### TC-21: 完整用户流程

**测试目标**: 验证完整的用户使用流程

**测试步骤**:
1. 打开 Gmail
2. 点击 FAB 按钮
3. 添加收件人
4. 输入主题
5. 撰写邮件内容(使用格式化)
6. 点击发送
7. 观察整个流程

**期望结果**:
- [ ] 所有步骤流畅
- [ ] 无明显问题
- [ ] 用户体验良好
- [ ] 发送成功

**截图**: [粘贴关键步骤截图]

---

## 📊 验收结果汇总

### AI 自动验收结果

**验收日期**: ___________
**验收人**: AI 工程师
**验收结果**: [ ] 通过  [ ] 不通过

**通过的检查项**: _____ / _____

**未通过的检查项**:
1.
2.
3.

---

### 人工验收结果

**验收日期**: ___________
**验收人**: 用户
**验收结果**: [ ] 通过  [ ] 不通过

**通过的测试用例**: _____ / 21

**失败的测试用例**:
1.
2.
3.

---

## 🐛 发现的问题

### 问题列表

1. **问题描述**:
   - 严重程度: [ ] P0  [ ] P1  [ ] P2
   - 复现步骤:
   - 期望行为:
   - 实际行为:

2. **问题描述**:
   - 严重程度: [ ] P0  [ ] P1  [ ] P2
   - 复现步骤:
   - 期望行为:
   - 实际行为:

---

## ✅ 最终验收决策

### 验收通过条件

- AI 自动验收全部通过
- 人工验收测试用例通过率 ≥ 90%
- 无 P0 级别的 Bug
- P1 级别的 Bug ≤ 2 个
- 完整发送流程正常工作

### Sprint 2 完成标准

任务 2.4 验收通过后,Sprint 2 的所有任务都已完成:
- ✅ 任务 2.1: 富文本编辑器集成
- ✅ 任务 2.2: 收件人管理功能
- ✅ 任务 2.3: 加密逻辑实现
- ✅ 任务 2.4: 发送流程集成

### 验收决策

**最终结论**: [ ] 通过  [ ] 不通过  [ ] 有条件通过

**决策说明**:

**后续行动**:
- [ ] 修复发现的 Bug
- [ ] 更新开发进度管理文档
- [ ] 执行 Sprint 2 整体验收
- [ ] 准备 Sprint 3 规划

---

**验收人签字**:
- AI 工程师: ___________
- 用户: ___________

**项目负责人签字**: ___________

---

## 📚 相关文档

- [2.4_任务执行卡_发送流程集成.md](./2.4_任务执行卡_发送流程集成.md) - 任务执行卡
- [2.Sprint2_写信面板开发.md](./2.Sprint2_写信面板开发.md) - Sprint 2 规划
- [2.验收卡_Sprint2_写信面板开发.md](./2.验收卡_Sprint2_写信面板开发.md) - Sprint 2 整体验收
- [开发进度管理.md](../开发进度管理.md) - 整体进度跟踪

---

**备注**: 本验收卡采用两阶段验收流程,AI 自动验收通过后才能进入人工验收。任务 2.4 是 Sprint 2 的最后一个任务,验收通过后即可进行 Sprint 2 的整体验收。
